'''somehow can be integrated, defining logging only here. Can be deleted'''
# import logging

# logging.basicConfig(format=u'%(filename)s [LINE:%(lineno)d] #%(levelname)-8s [%(asctime)s]  %(message)s',
#                     level=logging.INFO,
#                     # level=logging.DEBUG,
#                     )